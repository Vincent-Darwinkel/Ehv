apiVersion: v1
kind: Service
metadata:
  name: {{ template "eventdatepickerservice.fullname" . }}
  labels:
    app: {{ template "eventdatepickerservice.name" . }}
    chart: {{ template "eventdatepickerservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "eventdatepickerservice.name" . }}
    release: {{ .Release.Name }}
