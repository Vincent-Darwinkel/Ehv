apiVersion: v1
kind: Service
metadata:
  name: {{ template "event-datepicker-service.fullname" . }}
  labels:
    app: {{ template "event-datepicker-service.name" . }}
    chart: {{ template "event-datepicker-service.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "event-datepicker-service.name" . }}
    release: {{ .Release.Name }}
